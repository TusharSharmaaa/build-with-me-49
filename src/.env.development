# Keep ads off while we verify boot
VITE_ADS_MODE=off

# Safe placeholders so any service code doesn’t explode
VITE_SUPABASE_URL=https://placeholder.supabase.co
VITE_SUPABASE_ANON_KEY=placeholder

# If you see any other VITE_* in the repo, add a placeholder here the same way
# e.g.
# VITE_API_BASE=https://example.com
VITE_ADS_MODE=off
